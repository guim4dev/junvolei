# PRD v0.2 — Melhorias JunVolei

**Produto:** Simulador de Futevôlei 3D no Browser  
**Codinome:** JunVolei  
**Versão:** v0.2 (Melhorias de UX e Visual)  
**Status:** PRONTO PARA IMPLEMENTAÇÃO  
**Baseado em:** Feedback de playtesting v0.1

---

## 1. Resumo das Mudanças

Este PRD documenta 4 melhorias prioritárias identificadas durante testes:

| #   | Mudança                          | Tipo     | Prioridade |
| --- | -------------------------------- | -------- | ---------- |
| 1   | Saque estilo futevôlei           | Mecânica | Alta       |
| 2   | Nova bola (amarela com losangos) | Visual   | Alta       |
| 3   | Controles mobile com analógico   | UX       | Alta       |
| 4   | Corrigir WASD no desktop         | Bug fix  | Crítica    |

---

## 2. Detalhamento das Mudanças

### 2.1 Saque Estilo Futevôlei

**Problema:** Atualmente a bola simplesmente aparece no ar após um ponto.

**Solução:** Implementar saque igual ao futevôlei real - um jogador do time que vai sacar posiciona-se atrás da linha de fundo e chuta a bola.

**Comportamento esperado:**

```
┌─────────────────────────────────────────┐
│                  REDE                    │
│═════════════════════════════════════════│
│                                          │
│    [NPC 1]              [NPC 2]         │  ← Time Adversário
│                                          │
│─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─│
│                                          │
│    [VOCÊ]               [NPC 3]         │  ← Seu Time
│                                          │
└─────────────────────────────────────────┘
                    │
                    ▼  (linha de fundo)
               [SACADOR]  ← NPC do time que saca
                   ⚽
```

**Fluxo do saque:**

1. **Após ponto marcado:** Jogo pausa brevemente (1.5s)
2. **Posicionamento:** O sacador (NPC) se posiciona atrás da linha de fundo do seu lado
3. **Animação:** A bola aparece no chão em frente ao sacador
4. **Saque:** Após 1s, o sacador chuta a bola por cima da rede
5. **Rally inicia:** O jogo continua normalmente

**Regras de saque:**

- Time que PERDEU o ponto saca (padrão do futevôlei)
- Sacador: sempre um NPC (aliado se time do jogador saca, oponente caso contrário)
- Bola deve passar por cima da rede
- Se bola não passar a rede = ponto pro outro time

**Arquivos afetados:**

- `src/main.ts` — lógica de estado de saque
- `src/entities/NPC.ts` — adicionar comportamento de saque
- `src/systems/ScoreSystem.ts` — determinar quem saca
- Novo: `src/systems/ServeSystem.ts` — gerenciar mecânica de saque

**Pseudo-código:**

```typescript
// ServeSystem.ts
export class ServeSystem {
  private servingTeam: "player" | "opponent";
  private isServing: boolean = false;
  private serveDelay: number = 1500; // ms antes do saque
  private serverNPC: NPC | null = null;

  public startServe(team: "player" | "opponent") {
    this.servingTeam = team;
    this.isServing = true;

    // Selecionar NPC que vai sacar
    this.serverNPC = this.getServerNPC(team);

    // Posicionar sacador atrás da linha de fundo
    const serveZ =
      team === "player"
        ? GAME_CONFIG.COURT_LENGTH / 2 + 1 // Atrás da linha do jogador
        : -GAME_CONFIG.COURT_LENGTH / 2 - 1; // Atrás da linha do oponente

    this.serverNPC.setPosition(0, serveZ);

    // Posicionar bola no chão em frente ao sacador
    this.ball.reset(0, 0.3, serveZ - Math.sign(serveZ) * 0.5);
  }

  public executeServe() {
    // Calcular direção do saque (para o lado oposto)
    const direction = new THREE.Vector3(
      (Math.random() - 0.5) * 0.3, // Leve variação lateral
      0.8, // Arco para passar a rede
      this.servingTeam === "player" ? -1 : 1
    ).normalize();

    // Força do saque
    const serveForce = 12;

    this.ball.setVelocity(direction.multiplyScalar(serveForce));
    this.isServing = false;
  }
}
```

---

### 2.2 Nova Bola (Amarela com Losangos Pretos)

**Problema:** Bola atual é branca e muito pequena, difícil de ver.

**Solução:** Criar bola amarela com padrão de losangos pretos (estilo bola de futsal/futevôlei), ligeiramente maior que a cabeça do jogador.

**Especificações visuais:**

```
    Antes (v0.1)          Depois (v0.2)
    ┌─────────┐          ┌─────────────┐
    │    ⚪   │          │     🟡      │
    │ pequena │          │   ◇◆◇◆◇    │
    │ branca  │          │  grande     │
    └─────────┘          │  amarela    │
                         └─────────────┘
```

**Dimensões:**

```typescript
// Antes
BALL_RADIUS: 0.11; // Muito pequena

// Depois
BALL_RADIUS: 0.35; // ~70% do raio da cabeça do jogador (0.5)
// Diâmetro: 0.7 unidades
// Cabeça do jogador: ~1.0 unidade de diâmetro
```

**Implementação do padrão de losangos:**

Opção A: **Textura procedural (recomendada)**

```typescript
// Ball.ts
private createBallTexture(): THREE.CanvasTexture {
  const canvas = document.createElement('canvas');
  canvas.width = 256;
  canvas.height = 256;
  const ctx = canvas.getContext('2d')!;

  // Fundo amarelo
  ctx.fillStyle = '#FFD700'; // Amarelo dourado
  ctx.fillRect(0, 0, 256, 256);

  // Desenhar losangos pretos
  ctx.fillStyle = '#000000';
  const size = 32;

  for (let y = 0; y < 256; y += size * 2) {
    for (let x = 0; x < 256; x += size * 2) {
      // Losango
      ctx.beginPath();
      ctx.moveTo(x + size, y);
      ctx.lineTo(x + size * 2, y + size);
      ctx.lineTo(x + size, y + size * 2);
      ctx.lineTo(x, y + size);
      ctx.closePath();
      ctx.fill();
    }
  }

  const texture = new THREE.CanvasTexture(canvas);
  texture.wrapS = THREE.RepeatWrapping;
  texture.wrapT = THREE.RepeatWrapping;
  return texture;
}
```

**Cores atualizadas:**

```typescript
// constants.ts
export const COLORS = {
  // ...outras cores
  BALL_PRIMARY: 0xffd700, // Amarelo dourado
  BALL_SECONDARY: 0x000000, // Preto (losangos)
};
```

**Arquivos afetados:**

- `src/entities/Ball.ts` — nova geometria e textura
- `src/utils/constants.ts` — novas cores e tamanho

---

### 2.3 Controles Mobile com Analógico

**Problema:** Controles com setinhas (↑↓←→) são ruins em celular - imprecisos e desconfortáveis.

**Solução:** Substituir botões de seta por um joystick analógico virtual (estilo nipple.js ou similar).

**Layout atual (ruim):**

```
┌─────────────────────────────────────────┐
│            [ÁREA DO JOGO]               │
├─────────────────────────────────────────┤
│                                          │
│   ┌───┐                     ┌───────┐   │
│   │ ↑ │                     │ KICK  │   │
│   └───┘                     └───────┘   │
│ ┌───┐ ┌───┐                 ┌───────┐   │
│ │ ← │ │ → │                 │ HEAD  │   │
│ └───┘ └───┘                 └───────┘   │
│   ┌───┐                                  │
│   │ ↓ │                                  │
│   └───┘                                  │
└─────────────────────────────────────────┘
```

**Layout novo (melhor):**

```
┌─────────────────────────────────────────┐
│            [ÁREA DO JOGO]               │
├─────────────────────────────────────────┤
│                                          │
│   ╭─────────╮               ┌───────┐   │
│   │    ●    │               │ KICK  │   │
│   │   ╱│╲   │               └───────┘   │
│   │ ← ● → │               ┌───────┐   │
│   │   ╲│╱   │               │ HEAD  │   │
│   │    ●    │               └───────┘   │
│   ╰─────────╯                            │
│   (analógico)                            │
└─────────────────────────────────────────┘
```

**Comportamento do analógico:**

- **Área de toque:** Círculo de ~120px de diâmetro
- **Zona morta:** 10% do raio (evita movimento acidental)
- **Movimento:** Proporcional à distância do centro
  - Centro → 50% do raio = movimento normal
  - 50% → 100% do raio = sprint (movimento rápido)
- **Direção:** 360° (diagonal funciona naturalmente)
- **Visual:**
  - Base: círculo semi-transparente
  - Thumb: círculo menor que segue o dedo

**Implementação:**

```typescript
// TouchControls.ts (reescrito)
export class TouchControls {
  private joystickBase: HTMLDivElement;
  private joystickThumb: HTMLDivElement;
  private joystickActive: boolean = false;
  private joystickCenter: { x: number; y: number } = { x: 0, y: 0 };
  private moveX: number = 0;
  private moveZ: number = 0;
  private isSprinting: boolean = false;

  private createJoystick() {
    // Base do analógico
    this.joystickBase = document.createElement("div");
    this.joystickBase.style.cssText = `
      position: fixed;
      bottom: 40px;
      left: 40px;
      width: 120px;
      height: 120px;
      background: rgba(255, 255, 255, 0.2);
      border: 3px solid rgba(255, 255, 255, 0.4);
      border-radius: 50%;
      touch-action: none;
      z-index: 1000;
    `;

    // Thumb (parte que move)
    this.joystickThumb = document.createElement("div");
    this.joystickThumb.style.cssText = `
      position: absolute;
      width: 50px;
      height: 50px;
      background: rgba(255, 255, 255, 0.6);
      border-radius: 50%;
      left: 50%;
      top: 50%;
      transform: translate(-50%, -50%);
      pointer-events: none;
    `;

    this.joystickBase.appendChild(this.joystickThumb);
    document.body.appendChild(this.joystickBase);

    this.setupJoystickEvents();
  }

  private setupJoystickEvents() {
    const handleStart = (e: TouchEvent) => {
      e.preventDefault();
      this.joystickActive = true;
      const rect = this.joystickBase.getBoundingClientRect();
      this.joystickCenter = {
        x: rect.left + rect.width / 2,
        y: rect.top + rect.height / 2,
      };
      this.updateJoystick(e.touches[0]);
    };

    const handleMove = (e: TouchEvent) => {
      if (!this.joystickActive) return;
      e.preventDefault();
      this.updateJoystick(e.touches[0]);
    };

    const handleEnd = () => {
      this.joystickActive = false;
      this.moveX = 0;
      this.moveZ = 0;
      this.isSprinting = false;
      this.joystickThumb.style.transform = "translate(-50%, -50%)";
    };

    this.joystickBase.addEventListener("touchstart", handleStart);
    this.joystickBase.addEventListener("touchmove", handleMove);
    this.joystickBase.addEventListener("touchend", handleEnd);
    this.joystickBase.addEventListener("touchcancel", handleEnd);
  }

  private updateJoystick(touch: Touch) {
    const dx = touch.clientX - this.joystickCenter.x;
    const dy = touch.clientY - this.joystickCenter.y;
    const distance = Math.sqrt(dx * dx + dy * dy);
    const maxDistance = 60; // Raio máximo

    // Limitar ao raio máximo
    const clampedDistance = Math.min(distance, maxDistance);
    const angle = Math.atan2(dy, dx);

    // Posicionar thumb
    const thumbX = Math.cos(angle) * clampedDistance;
    const thumbY = Math.sin(angle) * clampedDistance;
    this.joystickThumb.style.transform = `translate(calc(-50% + ${thumbX}px), calc(-50% + ${thumbY}px))`;

    // Calcular movimento (zona morta de 10%)
    const deadZone = maxDistance * 0.1;
    if (distance < deadZone) {
      this.moveX = 0;
      this.moveZ = 0;
      return;
    }

    // Normalizar direção
    const normalizedDistance =
      (clampedDistance - deadZone) / (maxDistance - deadZone);
    this.moveX = Math.cos(angle) * normalizedDistance;
    this.moveZ = Math.sin(angle) * normalizedDistance; // Touch Y → World Z

    // Sprint se passar de 70%
    this.isSprinting = normalizedDistance > 0.7;
  }

  public update() {
    const speed = this.isSprinting ? 1.5 : 1.0;
    this.player.setMoveDirection(this.moveX * speed, this.moveZ * speed);
  }
}
```

**Arquivos afetados:**

- `src/controls/TouchControls.ts` — reescrever completamente

---

### 2.4 Corrigir WASD no Desktop

**Problema:** O controle WASD parou de funcionar no desktop.

**Diagnóstico provável:**

O `InputSystem` e `TouchControls` estão ambos chamando `player.setMoveDirection()`, e o `TouchControls` está sobrescrevendo com `(0, 0)` porque não há input touch.

**Código atual problemático:**

```typescript
// main.ts
function animate() {
  // ...
  if (gameState === "playing") {
    inputSystem.update(); // Define moveX, moveZ via WASD
    touchControls.update(); // Sobrescreve com (0, 0)! ← BUG
    // ...
  }
}
```

**Solução:**

1. Detectar se está em mobile ou desktop
2. Só ativar TouchControls em mobile
3. Ou: combinar os inputs (priorizar touch se ativo)

**Implementação recomendada:**

```typescript
// TouchControls.ts
export class TouchControls {
  private isActive: boolean = false;
  private isMobile: boolean;

  constructor(player: Player, ball: Ball) {
    this.isMobile = this.detectMobile();

    if (this.isMobile) {
      this.createJoystick();
      this.createActionButtons();
    }
  }

  private detectMobile(): boolean {
    return (
      /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(
        navigator.userAgent
      ) || "ontouchstart" in window
    );
  }

  public update() {
    // Só atualiza se estiver em mobile E joystick ativo
    if (!this.isMobile) return;
    if (!this.joystickActive && this.moveX === 0 && this.moveZ === 0) return;

    this.player.setMoveDirection(this.moveX, this.moveZ);
  }
}

// main.ts - ordem corrigida
function animate() {
  if (gameState === "playing") {
    // Desktop: InputSystem controla
    // Mobile: TouchControls controla (sobrescreve se ativo)
    inputSystem.update();
    touchControls.update();
  }
}
```

**Arquivos afetados:**

- `src/controls/TouchControls.ts` — adicionar detecção mobile
- `src/systems/InputSystem.ts` — verificar se não há conflito

---

## 3. Checkpoints de Implementação

### CP-01 — Fix WASD (Bug Crítico)

**Goal:** WASD funcionando no desktop

**Tasks:**

```bash
- Adicionar detecção mobile em TouchControls
- Só criar controles touch em mobile
- Testar WASD no desktop
- Testar touch no mobile (emulador)
```

**Success Criteria:**

- WASD move o jogador no desktop
- Touch controls aparecem só em mobile

**Commit:** `fix: WASD controls not working on desktop`

---

### CP-02 — Nova Bola

**Goal:** Bola amarela com losangos, maior

**Tasks:**

```bash
- Atualizar BALL_RADIUS em constants.ts (0.11 → 0.35)
- Criar textura procedural de losangos
- Aplicar textura na bola
- Ajustar física se necessário (bounce, etc)
```

**Success Criteria:**

- Bola visualmente maior e amarela
- Losangos pretos visíveis
- Bola ligeiramente maior que cabeça do jogador

**Commit:** `feat: new yellow ball with diamond pattern`

---

### CP-03 — Controle Analógico

**Goal:** Joystick virtual substituindo setas

**Tasks:**

```bash
- Remover código de botões de seta
- Implementar joystick base + thumb
- Implementar lógica de touch/drag
- Adicionar zona morta
- Adicionar visual feedback
- Testar em mobile
```

**Success Criteria:**

- Joystick aparece no canto inferior esquerdo
- Movimento suave e proporcional
- Direção 360° funciona
- Sprint funciona (arrastar longe)

**Commit:** `feat: replace arrow buttons with analog joystick`

---

### CP-04 — Sistema de Saque

**Goal:** Saque estilo futevôlei funcional

**Tasks:**

```bash
- Criar ServeSystem.ts
- Implementar estado de "serving"
- Posicionar NPC sacador atrás da linha
- Posicionar bola no chão
- Implementar animação de saque (NPC chuta)
- Conectar com ScoreSystem (quem perdeu saca)
- Testar fluxo completo
```

**Success Criteria:**

- Após ponto, NPC se posiciona para sacar
- Bola aparece no chão em frente ao sacador
- NPC chuta a bola por cima da rede
- Rally inicia normalmente

**Commit:** `feat: implement footvolley-style serve`

---

## 4. Ordem de Prioridade

```
1. CP-01 (Fix WASD)      ← Crítico, bloqueia testes
2. CP-02 (Nova Bola)     ← Visual, rápido
3. CP-03 (Analógico)     ← UX mobile importante
4. CP-04 (Saque)         ← Mecânica nova, mais complexa
```

---

## 5. Riscos

| Risco                                         | Impacto       | Mitigação                          |
| --------------------------------------------- | ------------- | ---------------------------------- |
| Textura de losangos não ficando boa em esfera | Visual ruim   | Testar UV mapping, ajustar padrão  |
| Joystick responsivo demais/de menos           | Difícil jogar | Ajustar zona morta e sensibilidade |
| Saque muito forte/fraco                       | Injusto       | Parametrizar força, testar valores |
| Conflito touch/keyboard                       | Bugs          | Separar bem os sistemas de input   |

---

## 6. Testes Recomendados

### Desktop

- [ ] WASD move o jogador em todas direções
- [ ] Espaço chuta a bola
- [ ] E cabeceia a bola
- [ ] ESC pausa o jogo

### Mobile (emulador ou dispositivo)

- [ ] Joystick aparece no canto inferior esquerdo
- [ ] Arrastar joystick move jogador proporcionalmente
- [ ] Arrastar longe faz sprint
- [ ] Botões KICK e HEAD funcionam
- [ ] Sem controles de seta visíveis

### Gameplay

- [ ] Bola é claramente visível (amarela, grande)
- [ ] Após ponto, saque acontece corretamente
- [ ] Saque passa por cima da rede
- [ ] Rally flui naturalmente após saque

---

## 7. Comando Ralph Loop

Para executar este PRD:

```bash
/ralph-loop:ralph-loop "Leia o PRD em ./prds/v0.2-melhorias.md. Implemente as melhorias do JunVolei seguindo os checkpoints em ordem (CP-01 a CP-04). Para cada checkpoint: 1) Implemente o código necessário 2) Teste manualmente se funciona 3) Faça commit. Priorize CP-01 (fix WASD) pois é crítico. Ao completar todos os checkpoints, emita <promise>COMPLETE</promise>" --completion-promise "COMPLETE" --max-iterations 50
```

---

**Última atualização:** Janeiro 2026  
**Autor:** Feedback de playtesting 🏐📱
